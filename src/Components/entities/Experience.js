export class Experience {
  img
  title
  description
  link
  constructor(props) {
    this.img = props.img
    this.title = props.title
    this.description = props.description
    this.link = props.link
  }
}